def Count(x,s):
    return len([1 for e in s if e == x])

def Percentages(alphabet,string):
    return [e,round((count/len(string))*100)) for alphabet in string 
    #l = []
    #for e in alphabet:
        #count = 0
        #for f in string:
            #if e == f:
                #count += 1
        #l += [(e,round((count/len(string))*100))]
    #return l

def RandomString(lenght,distribution):
    s = []
    for e in distribution:
        

    for e in range(len(10))
